# NISM Backend

## Run
1. Install dependencies: `npm install`
2. Start dev server: `npm run dev`
3. Health check: `GET /health`
4. DB ping: `GET /db/ping`

## Environment
Create a `.env` file using `.env.example`.
